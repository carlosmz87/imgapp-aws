pipeline{
    agent any 
    stages{
        stage('deploy'){
            steps{
                echo '--------deploy app on aws---------'
                sh 'aws --version'
            }
        }
    }
}